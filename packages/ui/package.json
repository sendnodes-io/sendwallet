{
  "name": "@my/ui",
  "version": "0.0.1",
  "sideEffects": [
    "*.css"
  ],
  "private": true,
  "types": "./types/index",
  "main": "src/index.tsx",
  "module:jsx": "src",
  "files": [
    "types",
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist types",
    "build": "tamagui-build",
    "watch": "tamagui-build --watch",
    "dev": "tamagui-build --watch --dev",
    "lint": "tsc --noEmit && yarn build"
  },
  "dependencies": {
    "@tamagui/animations-react-native": "^1.2.4",
    "@tamagui/font-inter": "^1.2.4",
    "@tamagui/react-native-media-driver": "^1.2.4",
    "@tamagui/shorthands": "^1.2.4",
    "@tamagui/theme-base": "^1.2.4",
    "tamagui": "^1.2.4"
  },
  "devDependencies": {
    "@tamagui/build": "^1.2.4"
  }
}
